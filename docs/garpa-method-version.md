# GARPA method version

Current branch method version: `1.7.0-commons-seeded-external-distribution`.

The highest governed stage is `commons_seeded_external_distribution`. It recomputes the qualified public-registry predecessor, binds the sole current governing release and complete file set, distinguishes a non-public qualification fixture from an observed external event, and requires content-addressed platform, retrieval, and registry evidence before recording public-release or public-registry flags.

A qualification fixture does not establish that publication occurred. An observed external event does not establish deployment authority or unrestricted product equivalence.
