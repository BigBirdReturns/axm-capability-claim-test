# Commons-seeded external-distribution qualification fixture

This directory is generated by `scripts/generateCommonsSeededExternalDistributionExample.ts`.

The generated request uses `qualification_fixture` mode and an inaccessible `urn:garpa:qualification-fixture:` destination. It exercises the complete receipt, file-set, evidence-artifact, chronology, digest, and boundary logic without asserting that any external publication or distribution occurred.

Synthetic observed-event requests exist only inside the regression tests. They verify the positive observed-event path but are not operational receipts and must not be represented as real publication evidence.
