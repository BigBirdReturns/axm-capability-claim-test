{
  "name": "axm-capability-claim-test-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server exposing the Capability Claim Test method (object gate, sourcing gate, contamination bucket, verdict) as tools any frontier AI client can call.",
  "bin": {
    "capability-claim-test-mcp": "server.ts"
  },
  "scripts": {
    "start": "tsx server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "tsx": "^4.19.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
