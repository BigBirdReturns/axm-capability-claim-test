{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.org/axm-capability-claim-test/claim.schema.json",
  "title": "Claim",
  "description": "One claim against a load-bearing field. Never upgrade beyond the source.",
  "type": "object",
  "required": ["field", "statement", "evidenceClass", "sourceIds"],
  "properties": {
    "id": { "type": "string" },
    "field": {
      "type": "string",
      "description": "Load-bearing field key, e.g. capital_raised, fund_size."
    },
    "statement": { "type": "string" },
    "evidenceClass": {
      "type": "string",
      "enum": ["confirmed", "reported", "derived", "judgment", "open"]
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Analyst confidence. Internal only; never travels as a verdict."
    },
    "sourceIds": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Source ids. A claim is sourced iff it cites >=1 source and is not class 'open'."
    },
    "notes": { "type": "string" }
  },
  "additionalProperties": false
}
